interface IRevenueCatResponse {
    subscriber: {
        subscriptions: {
            expirybusiness_monthly_default_1person?: {
                expires_date: string;
                purchase_date: string;
                store: string;
            };
            expirybusiness_monthly_default_3people?: {
                expires_date: string;
                purchase_date: string;
                store: string;
            };
            expirybusiness_monthly_default_5people?: {
                expires_date: string;
                purchase_date: string;
                store: string;
            };
            expirybusiness_monthly_default_10people?: {
                expires_date: string;
                purchase_date: string;
                store: string;
            };
            expirybusiness_monthly_default_15people?: {
                expires_date: string;
                purchase_date: string;
                store: string;
            };
        };
    };
}
